# index

Landing page that lists all aeratools with icons, descriptions, and links.

Tools are read from `/tools.json` at the repo root (baked in at build time). Each tool card links to `https://<toolname>.<ROOT_DOMAIN>` and tools are grouped by their `group` field. Icons use Bootstrap Icons.
